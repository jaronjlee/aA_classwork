<h1>USERS INDEX</h1>
<%# controller gives us @users so we can iterate through that to get each user %>
<%# pe + tab = returning erb %>
<%# er + tab = non returning erb %>
<a href="<%= new_user_url %>">Go to New User Form</a>

<ul>
  <% @users.each do |user| %>
    <li>
      <%= render 'user', banana: user, action: :index %>
      <%# 'user' == name of partial %>
      <%# dark blue banana symbol == setting key of banana for our partial to access%>
      <%# light blue user (value) is from user in pipes of iteration %>
    </li>
  <% end %>
</ul>